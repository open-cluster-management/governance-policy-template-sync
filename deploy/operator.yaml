apiVersion: apps/v1
kind: Deployment
metadata:
  name: governance-policy-template-sync
spec:
  replicas: 1
  selector:
    matchLabels:
      name: governance-policy-template-sync
  template:
    metadata:
      labels:
        name: governance-policy-template-sync
    spec:
      serviceAccountName: governance-policy-template-sync
      imagePullSecrets:
        - name: multiclusterhub-operator-pull-secret
      containers:
        - name: governance-policy-template-sync
          image: quay.io/open-cluster-management/governance-policy-template-sync:1.0.0-PR2-91d849553599bd4f67a8582baa3973cc71272ed3
          command:
          - governance-policy-template-sync
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              value: calamari
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "governance-policy-template-sync"
